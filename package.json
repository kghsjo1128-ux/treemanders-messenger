{
  "name": "treemanders-hosting",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "deploy": "node --no-deprecation ./node_modules/firebase-tools/lib/bin/firebase.js deploy --only hosting",
    "deploy:all": "node --no-deprecation ./node_modules/firebase-tools/lib/bin/firebase.js deploy",
    "watch:hosting": "node watch-hosting-deploy.mjs"
  },
  "dependencies": {
    "firebase": "^11.0.0"
  },
  "devDependencies": {
    "chokidar": "^4.0.3",
    "firebase-tools": "^13.0.0"
  }
}
